import { XhrFilterConfig } from "../interface/xhr-filter-config";
import { HttpRequest, HttpResponse, XhrContext } from '../models';
export declare class RxHttpResponse {
    private request;
    private filters;
    private inMemoryFilter;
    private onError;
    private badRequest;
    private _index;
    private _onSuccess;
    private _onError;
    private _onResponse;
    constructor(request: HttpRequest, filters: XhrFilterConfig[], inMemoryFilter: XhrFilterConfig, onError: (response: HttpResponse) => void, badRequest: (result: any) => void);
    process(type: string, success?: any): void;
    private executeFilterOnRequest;
    private callFilter;
    private onComplete;
    onRequestExecuting: () => (context: XhrContext) => void;
    onRequestError(response: any): void;
}
