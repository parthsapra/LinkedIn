import { Injector } from "@angular/core";
import { CanActivate, ActivatedRouteSnapshot, RouterStateSnapshot } from "@angular/router";
import { ParamResolver } from "../core/param-resolver.service";
export declare class BaseCanActivate implements CanActivate {
    private injector;
    private paramResolver;
    constructor(injector: Injector, paramResolver: ParamResolver);
    canActivate(route: ActivatedRouteSnapshot, state: RouterStateSnapshot): boolean | Promise<boolean>;
    resolveRoute(resolve: any, route: ActivatedRouteSnapshot): (value: boolean) => void;
}
